# Set this to the trusted HTTPS origin when deploying.
SITE_URL=http://localhost:5173

# Same-origin Better Auth endpoint. Use your trusted HTTPS origin in production.
BETTER_AUTH_URL=http://localhost:5173
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
