import { EditorFrame } from "@/components/renderers/editor-frame";
export const metadata = {
  title: "Editor preview — ThemeSpace",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <EditorFrame />;
}
